#ifndef RECORD_H
#define RECORD_H

typedef struct {
    // Define your record structure here
    // For example, assuming a record has an ID and a value
    int id;
    char last_name[20];
    char first_name[20];
    int value;
} Record;

#endif // RECORD_H